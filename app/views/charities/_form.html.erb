<%= form_with(model: charity, local: true) do |f| %>
  <% if charity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user_charity from being saved:</h2>

      <ul>
      <% charity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container">
        <div class="row centered-form">
        <div class="col-xs-12 col-sm-8">
        	<div class="panel panel-default">
        		<div class="panel-heading">
              <h3 class="panel-title">Join PTH's Network of Charities</h3>
			 			</div>
            <div class="panel-body">
              <div class="form-group row">
                <label for="charity_name" class="col-sm-5 col-form-label">Charity Name</label>
                <div class="col-sm-7">
                  <%= f.text_field :first_name, class: 'form-control' %>
                </div>
              </div>

              <div class="form-group row">
                <label for="email" class="col-sm-5 col-form-label">Email</label>
                <div class="col-sm-7">
                  <%= f.text_field :email, class: 'form-control' %>
                </div>
              </div>

              <div class="form-group row">
                <label for="password" class="col-sm-5 col-form-label">Password</label>
                <div class="col-sm-7">
                  <%= f.text_field :password, class: 'form-control' %>
                </div>
              </div>

              <div class="form-group row">
                <label for="password_confirmation" class="col-sm-5 col-form-label">Confirm Password</label>
                <div class="col-sm-7">
                  <%= f.text_field :password_confirmation, class: 'form-control' %>
                </div>
              </div>

              <div class="field form-group">
                <%= f.submit "Submit", :class => "btn btn-info btn-block" %>
              </div>
                    </div>
                  </div>
                </div>
  			    	</form>
  			    </div>
  	    	</div>
      	</div>
      </div>
    </div>
<% end %>
